import React, { Component } from 'react';

class Search extends Component {
    render() {
        return (
            <input type="text" name="Search" placeholder="Search a Song" />
        );
    }
}

export default Search;